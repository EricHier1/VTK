This repo is for learning and practicing git commands. A few git commands I have learned and am gaining proficiency with are:

git init
git init [new directory name]
git commit -m "commit message"
git push
git pull
git cp
git help
git help config
git config --global user.name "User Name"
git config --global user.email "email"
cat ~/.gitconfig
git status
git commit -a                   adds and commits at same time
git log
git log --oneline
git show [log number/hash]
git reset HEAD [filename]
git checkout -- [filename]

##Backout Changes already staged 

git rm
git restore
git mv

git clone "url"
git push origin main
git push

git config --global init.defaultBranch main
git config --global list

